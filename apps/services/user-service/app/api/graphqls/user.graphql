type User {
  id: ID!
  email: String!
  role: String!
  createdAt: String!
  updatedAt: String!
}

input CreateUserInput {
  email: String!
  password: String!
}

type Query {
  health: String!
}

type Mutation {
  createUser(input: CreateUserInput!): User!
}
